# vitest >=4.1 lists @vitest/browser-playwright as an optional peer pinned to its own
# version. npm 10 resolves that optional peer set against the latest release, which
# peers on vitest 5, and its resolver crashes on the conflict ("reading 'edgesOut'").
# Every peer this project actually needs is already a direct dependency.
legacy-peer-deps=true
